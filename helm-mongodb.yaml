architecture: standalone

auth:
  enabled: true
  rootUser: root
  rootPassword: "<strong-mongodb-root-password>"

persistence:
  enabled: true
  storageClass: ebs-gp3
  accessModes:
    - ReadWriteOnce
  size: 8Gi

service:
  type: ClusterIP

resources:
  requests:
    cpu: 100m
    memory: 256Mi
  limits:
    cpu: 500m
    memory: 512Mi
